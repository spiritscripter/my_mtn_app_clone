name: my_mtn_app
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.2.3 <4.0.0"

dependencies:
  auto_route: ^8.0.3
  flutter:
    sdk: flutter

dev_dependencies:
  auto_route_generator: ^8.0.0
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/

  fonts:
    - family: GillSans
      fonts:
        - asset: fonts/gill_sans_mt.ttf
